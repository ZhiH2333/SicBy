# Application

Purpose: Orchestration layer (controllers, workflows). No platform APIs.

TODOs
- [CodeX] Specify controller contracts (LibraryController, PlaybackController, MetadataController).
- [Shared] Define event flow between ingestion, metadata, and playback.
